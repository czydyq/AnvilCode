# AnvilCode · 简历项目经历（6 条版）

> 本文档基于你提供的项目描述重写，把「核心功能 7 条 + 项目难点 6 条」压缩为 **6 条简历条目**。
> 每条均已对照仓库代码核实，量化数据可用 `make verify` / `make experiments` / `make real-model-e2e` 当场复现。

---

## 一、项目经历（6 条 · 可直接粘贴）

**AnvilCode —— 本地 AI Agent 运行时**（个人项目，独立开发）
2026.05 – 2026.09

**技术栈**：Python 3.12 · asyncio · Pydantic v2 · JSON-RPC 2.0 over NDJSON/TCP · Claude API · MCP · Textual · pytest / mypy(strict) · ruff · uv

**项目简介**：面向开发者的本地 AI Agent 运行时。以「常驻守护进程 + 终端客户端」双进程架构替代主流单进程形态，实现多 Agent 协作、工具调用与 LLM 流式处理；用户通过终端与守护进程交互，Agent 循环调用 Claude API 并执行工具，全过程以类型化事件实时推送至前端渲染。从零开发至支持 Skills、MCP 协议与多客户端订阅同一事件流。

**核心工作：**

1. **设计守护进程 + 多客户端双进程架构，并把可观测性做成一等公民。** `anvil-core` 常驻运行、`anvil-tui` 与 CLI 作为可断开重连的客户端，任务执行不依附于发起连接；事件总线基于 Pydantic v2 discriminated union 定义类型化事件，支持 fnmatch 主题过滤（glob topic）与 `global` / `run:<id>` 作用域订阅，多客户端可并行订阅同一事件流。将推理、流式输出、工具调用、权限审批、子 Agent 执行统一抽象为事件，前端据此实时渲染——TUI 支持流式逐 token 输出、子 Agent 层级进度（缩进 + `└─` 连接符）、Slash 命令自动补全。**实测强杀客户端连接后任务仍达 success，新连接按 run_id 回放补齐 14 条历史事件。**

2. **实现稳定的 Agent Loop 与 LLM 流式链路。** 设计「推理 → 工具调用 → 结果观察 → 继续推理」闭环，支持单步多工具调用与多轮迭代；封装 Claude API，逐 token 发布事件供前端流式渲染，网络异常时自动重试（最多 3 次、退避 1s/2s/4s，且仅在首次尝试发布 token 事件以避免前端重复渲染）。针对循环失控设计多层兜底：最大步数限制、`end_turn` 优先于 `max_steps` 判定终止、工具执行出错**不中断循环**而是把错误文本回填给模型自行纠错、输出被 `max_tokens` 截断在工具调用中间时补一条合成错误结果以保持消息序列平衡。

3. **实现插件式工具系统与完整的工具调用链路。** `ToolRegistry` 支持工具注册、发现与路由，每个工具以 `BaseTool` 抽象 + Pydantic `params_model` 声明参数边界、运行时统一校验；单次调用链路为「schema 校验 → 权限审批 → 限时执行 → 失败分类 → 指数退避重试」，仅 `runtime_error` / `rate_limited` 重试，失败一律转为错误结果交回模型。**共实现 11 个工具**（read_file / write_file / list_dir / bash / note_save / 任务系统 ×4 + spawn_agent / agent_result）。

4. **设计本地 Agent 的权限控制与安全边界。** `PermissionManager` 在每次工具调用前评估策略，支持 `allow_once` / `always_allow` / `deny_once` / `always_deny` 四种审批决策并持久化到 `policy.toml`。安全设计的关键在**评估顺序**：把「工作目录越界强制审批」置于 session 级与持久化缓存**之前**，使「永久允许」无法成为绕过通道；越界启发式覆盖绝对路径、`~`、`..` 跳转、`$HOME`/`$PWD` 展开与显式 `cd`。审批走 `Future` + 超时挂起，客户端断连时统一 resolve 为拒绝以避免协程永久挂起。**实测审批挂起态下探活往返 0.6 ms**，从机制上消除「越危险的操作越容易因审批机制自身而失败」的死锁。

5. **实现长任务上下文治理机制。** 以真实 `usage.input_tokens` 而非字符估算计算上下文水位，触阈值后自动压缩为六段式自包含交接摘要（目标 / 已完成 / 关键约束 / 文件状态 / 剩余 TODO / 关键数据），在保留任务目标与关键决策的前提下显著降低历史消息长度；摘要落盘留痕并发出带原始/摘要 token 数的可审计事件。**压缩时机是设计的一部分**：严格卡在「工具结果追加完毕且消息尾部为 user」之后，保证压缩产物对下一次 API 调用是合法消息序列，压缩失败时原始上下文不变（无损）。超长工具结果按预算截断并**指明原文在 run events 中的位置**（13000 → 4053 字符），避免静默丢信息。

6. **实现多 Agent 协作编排与扩展生态。** `SpawnAgentTool` 支持父 Agent 派生子 Agent 执行独立任务：冷启动隔离上下文（工具描述中显式声明「看不到父对话，必须写全」）、前台阻塞与后台并行两种模式、嵌套深度上限、子事件桥接至父总线以便前端渲染层级进度、后台任务经注册表注册后用 `agent_result` 轮询取结果。同时实现 Skills 系统（Markdown + YAML front-matter，三级覆盖，`/skill-name` 触发，可用 `allowed_tools` 收敛工具白名单）与 MCP 客户端（JSON-RPC 2.0 over stdio/TCP 双传输，异步执行并自动超时取消，`stderr` 后台持续排空以防子进程管道阻塞）。

---

## 二、备选第 7 条（空间允许时强烈建议加上）

你原描述里没有这条，但它是整个项目**最难被替代的差异化**——第 1–6 条证明的是「你做出了这些功能」，这一条证明的是「你有工程方法论」。

7. **建立「假模型受控实验 + 真实模型现场验证」双层验证方法论。** 将 `ANTHROPIC_BASE_URL` 指向本地确定性假模型服务，**在不改动任何一行生产代码的前提下接管模型层**，其余全部走生产路径（真实守护进程、真实 IPC、真实工具执行、真实权限审批、真实压缩器），从而可复现、零成本地构造「上下文刚好越过压缩阈值」这类真实模型下无法精确命中的边界条件。据此把设计主张写成 **7 条可证伪命题，7/7 全部被实验证实**；现场验证则在真实模型 + 真实审批交互下确认同一套机制在模型不确定性下依然成立。另将协议文档改为**由 Pydantic 模型生成并纳入验证门禁**，使文档漂移必然导致验证失败。**工程规模：生产代码 7,234 行 / 85 个文件，测试代码 5,141 行 / 272 个用例全绿，`mypy --strict` 与 `ruff` 全通过。**

---

## 三、个人收获（压缩为 3 条 · 用于自我介绍，**不要**放进简历项目栏）

你给了 8 条收获，我压成 3 条。**但请注意语态问题**：那 8 条原文是「深入理解了…」「对…有了更深认识」——这是学习日志的语态，描述的是你的认知变化，而不是你的产出。放在简历的「项目经历」里是弱表述：面试官无法验证，也不关心。这类内容的正确位置是**自我介绍或技术面自述**，且应改成「我能做什么」的语态。

1. **Agent 机制层面**：能独立设计并实现 ReAct / Tool Use 式的 Agent Loop 闭环，包括工具 schema 描述、调用分发、结果回传、多轮迭代控制，以及循环失控、异常中断、输出截断等工程兜底。
2. **Agent 安全层面**：理解本地 Agent 在执行文件操作、Shell 命令、外部工具前必须引入权限审批与策略持久化的原因，并能从**评估顺序**这一层设计出不可被便利性缓存绕过的安全边界。
3. **系统架构层面**：对 Claude Code 类产品的底层原理有系统理解——流式输出、工具调用、权限审批、Skills 扩展、多 Agent 协作、上下文压缩与 MCP 生态解耦，能从架构角度拆解 Agent 如何完成复杂任务。

---

## 四、面试口径（每条 → 高概率追问 → 怎么答）

| 条目 | 高概率追问 | 回答要点 |
|---|---|---|
| ① 双进程架构 | 「单进程不是更简单？为什么付 IPC 的代价？」 | 承认代价（协议演进、序列化、并发、错误传播）。收益是**结构性消除**一类失效：单进程下「前端消失即任务消失」无法靠断线重连补丁解决，且多前端可复用同一套通道 |
| ② Agent Loop | 「循环失控具体怎么兜底？」 | 分四层答：`max_steps` 上限、`end_turn` 优先判定、错误回填让模型自纠、`max_tokens` 截断时补合成结果保持消息序列平衡。最后一层最能体现对 API 语义的掌握 |
| ③ 工具系统 | 「工具调用失败会不会中断整个 run？」 | 不会——失败转为错误结果交回模型，让它自行纠错；且做了失败分类，只有 `runtime_error` / `rate_limited` 重试，其余直接返回，避免无意义重试 |
| ④ 权限控制 | 「为什么不按规则特异性排序，而按层级顺序？」 | 明确这是与 Claude Code `deny → ask → allow` 三表首命中的**不同实现路线**：对方靠规则易读，本项目靠**层级顺序**保证不可绕过。并说明取舍是有意接受更多审批打扰，因为打扰落在越界一侧、对应不可逆风险 |
| ⑤ 上下文治理 | 「压缩时机为什么必须卡在那个位置？」 | 讲 API 语义约束：替换后必须构成合法消息序列（`tool_use` / `tool_result` 严格配对、尾部消息角色）。**位置是设计的一部分，不是实现细节**；另说明失败时返回 None 且上下文不变，压缩是无损的 |
| ⑥ 多 Agent / MCP | 「子 Agent 为什么要冷启动隔离上下文？」 | 是**上下文预算控制**：子 Agent 只把结果交回父会话，避免父上下文被子任务的过程细节污染。同时说明代价（子 Agent 看不到父对话，工具描述里必须显式写全） |
| ⑦ 验证方法论 | 「假模型实验能证明什么？不能证明什么？」 | 诚实区分：受控实验证明的是**可证伪性**（能构造边界、能复现），不能证明真实模型的不确定性下仍成立——所以有第二层现场验证补上。并主动说明现场验证走的是 Anthropic **兼容网关**而非官方端点 |

---

## 五、量化数据速查表

| 指标 | 数值 |
|---|---|
| 生产代码规模 | 7,234 行 / 85 个 Python 文件 |
| 测试规模 | 5,141 行 / 272 个用例全部通过 |
| 静态检查 | `mypy --strict` 85 个源文件 0 问题；`ruff` 全通过 |
| 工具数量 | 11 个（9 内建 + `spawn_agent` + `agent_result`） |
| 权限审批决策 | 4 种（allow_once / always_allow / deny_once / always_deny）+ `policy.toml` 持久化 |
| 权限评估链 | 6 级，越界检测优先级高于全部缓存 |
| 前端断开后任务存续 | 强杀连接后 status=success，回放补齐 **14 条**历史事件 |
| 审批挂起态并发能力 | 探活往返 **0.6 ms** |
| 流式重试 | 最多 3 次，退避 1s / 2s / 4s |
| 压缩触发（真实水位） | original_tokens=2405 → summary_tokens=20，带可审计事件 |
| 工具结果截断 | 13000 字符 → **4053 字符**（保留前缀 + 原文检索指引） |
| 设计命题验证 | **7 条可证伪命题 7/7 证实** |
| 现场验证 | 真实模型 + 真实守护进程 + 真实审批，2 步 / 2.2 s 达 success，经第三方 Anthropic 兼容网关跑通流式、工具调用与 usage 解析全链路 |
| 交付节奏 | 8 个阶段（s0 → s7）递进交付：协议层 → 核心闭环 → 双进程 → 任务系统 → 会话记忆 → 安全审批 → 记忆压缩 → 扩展生态 |

---

## 六、诚实边界（**不写进简历**，但必须能当场承认）

面试中这类项目最容易在「有什么不足」上失分。以下四条最值得准备，**建议面试时主动提及第一条**：

1. **本地信任边界缺失（最严重）**：`SocketServer` 监听 TCP 但无鉴权，本机任意进程连上即可调用 `permission.respond` **自行批准**工具调用。缓解方向是 token 握手或改用 Unix domain socket + `0600`。这条恰好会削弱第 4 条的权限亮点，被面试官自己挖出来会很被动——主动指出并给出缓解方向反而加分。
2. **事件总线缺背压且消费语义不一致**：`EventBus.publish` 串行 await 所有订阅者，一个卡住的客户端会拖慢 Agent 循环；`EventWriter` 未按 `run_id` 过滤，并发 run 会产生事件交叉污染；`EventBus` 缺少 `unsubscribe`，长跑进程中订阅者会随 run 数线性增长。
3. **权限策略只覆盖 `bash`**：路径级正则规则对 `write_file` 无效，尚缺「写文件限制在 workspace 内」「拒写 `.env` / `.git/`」这类策略。
4. **压缩会丢掉最近工作集**：当前把全部消息压成摘要对，刚读的文件内容与刚看到的报错会丢失，模型需重读；计划保留最近 K 轮原文、只压更早部分。

次要项两条：`run` 进行中不截断 `tool_result`（截断仅在会话重载时生效）；模型 context window 硬编码，换用未登记模型会按 200k 计算导致压缩触发时机不准。

---

## 七、使用说明 · 需要你确认的三件事

**1. 项目名称：你写的是 KamaClaude，仓库当前是 AnvilCode。** 仓库已完成改名（`src/kama_claude/` → `src/anvil_code/`，可执行入口为 `anvil` / `anvil-core` / `anvil-tui`），源码与 README 中已无 `kama` 残留。本文档统一使用 **AnvilCode**，因为面试官若去仓库核对，看到的是这个名字。**若你在别处仍以 KamaClaude 名义投递，请自行统一，不要两个名字混用。**

**2. 作者归属（重要，请先核对）。** 本仓库 git 历史的作者分布为：`charon2121` **22 次提交**（含 s0→s7 全部功能开发）、`youngyangyang04` / `程序员Carl` **3 次提交**（初始提交与后续 Update）。因此本文档默认使用「个人项目，独立开发」的措辞——**若实际为团队协作或存在导师 / 上游仓库，请改为「核心开发，负责 X 模块」并明确你的实际职责边界**。这部分面试官一查 commit 记录就会露底，绝不能含糊。

**3. 时间与文件位置。** 时间按「2026.05 – 2026.09」填写（与仓库首次提交及当前开发状态一致），请按实际投入调整。`RESUME.md` 位于仓库根目录且未被 `.gitignore` 忽略，若不希望它进入公开仓库，请移动到仓库外或加入 `.gitignore`。

**4. 裁剪建议。** 六条版适合作为简历的独立项目经历条目；若整份简历只给这个项目 2–3 行空间，建议保留第 1、4、7 条（架构差异化 + 安全设计 + 方法论），其余合并压缩。第三、四、六节的面试准备内容不要放进简历。
